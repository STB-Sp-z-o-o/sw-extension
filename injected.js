// injected.js

// This script is injected into the main page context to bridge the gap
// between the content script's isolated world and the chrome runtime APIs.

// Remove SharePoint search event bridge logic.
